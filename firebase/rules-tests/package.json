{
  "name": "petdate-firestore-rules-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test --test-timeout=60000 rules.test.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0"
  }
}
